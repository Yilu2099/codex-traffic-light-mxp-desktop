# 万合创新局 Mac 客户端开发与更新规则

## 开发基线

1. 新任务 fetch 后核对基线并创建独立 worktree；同一任务已有的隔离 worktree 可以续做，合并/发布前再同步。共享目录不自动 pull 或切分支；历史发布目录不能直接作为当前发布源。
2. 不覆盖其他任务的改动，不全量暂存或 force-push。局部迭代运行受影响验证；代码发布前运行仓库提供的完整 Swift 测试、构建检查和 `git diff --check`。纯文档改动检查引用与差异。
3. 版本号必须从仓库 `VERSION` 和服务器实时 manifest 动态读取，不把某个 1.2.x 版本写成长期判断条件。

## 安装与自动更新

1. 成员首次接入或修复时，先检查 `~/.wanhe-codex-token/config.env`、当前版本、`com.codex.traffic-light-mxp`、`com.codex.traffic-light-mxp-updater`、更新日志和服务器签名 manifest。
2. 已安装且更新器健康时，只使用服务器发布的 SHA-256 与签名校验更新，不重新克隆、不修改源码、不自编译安装包。只有未安装、配置错误或更新器损坏时才运行仓库正式安装/修复脚本。
3. 更新必须保留成员身份、邀请码换取的设备凭证、采集缓存和服务器地址；任何输出、日志或提交都不得泄露邀请码、设备令牌或签名私钥。
4. 发布后分别验证 GitHub `main`、服务器 manifest、下载包哈希/签名、灰度或强制策略、目标设备 `current` 版本、两个 LaunchAgent 和实际上报端点。某一项没有实际证据就标明未验证。

## 多端契约

- 修改额度、Token、工作日、项目审计、成员资料或版本字段时，同时核对网站 API 和管理后台契约。
- 成员提示词只负责首次接入或修复；以后历次版本由独立签名更新器接管，不要求成员重复发送安装对话。

## 完成与授权

在本次授权范围内完成实现、验证与失败修复；不在每次本地检查前问批准。界面变化先给真实预览，用户确认后再提交、推送或发布；已有授权不重复确认。安装、更新与正式发布按本次范围执行，不把文档修改等同于更新成员设备。不为纯文档修改升版本。
